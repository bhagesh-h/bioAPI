# Development and CI dependencies.
# Installed on top of the runtime set inside the container's `dev` stage.

-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
httpx==0.28.1

ruff==0.16.3
mypy==2.3.1
